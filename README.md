# jebwc

A clone of GNU wc, written with Flex

Type "make" to compile

Program reads data on stdin, example:

./jebwc < bigfile.txt

## Backstory

This is basically "student's first Flex program" in school, I think I wrote this based off of an example program in a compilers class.
